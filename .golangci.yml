version: "2"

linters:
  default: standard
  enable:
    # The GitHub adapter owns raw diff response bodies.
    - bodyclose
    # Exported identifiers carry the command contract; keep them documented.
    - revive
  settings:
    revive:
      rules:
        - name: exported
